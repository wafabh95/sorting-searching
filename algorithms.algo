PROCEDURE sort(VAR arr : ARRAY_OF INTEGER, i, j : INTEGER)
VAR
   i,j,value: INTEGER;
BEGIN
  FOR i FROM 1 TO n-1   DO
        value := arr[i];
        j = i  ;
        WHILE (j > 0) and arr[j-1] > value
            (arr[j+1] := arr[j]);
                j = j-1;
        END_WHILE  
  
  END_FOR
END